<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/im_recipe_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:contentDescription="@string/recipe_thumbnail_image"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/tv_recipe_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/im_recipe_thumbnail"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:maxLines="1"
        android:padding="16dp"
        android:textSize="16sp"
        tools:text="Brownies"/>

    <ImageView
        android:id="@+id/im_favourite_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/tv_recipe_name"
        android:layout_margin="8dp"
        android:contentDescription="@string/favourite_button_image"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_favorite_border_white"/>

</RelativeLayout>